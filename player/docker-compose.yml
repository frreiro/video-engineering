services:
  nginx-lowlatency:
    image: nginx:1.27-alpine
    container_name: encoding-bitrate-nginx-lowlatency
    ports:
      - "8080:8080"
      - "8000:8090"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./www:/usr/share/nginx/html:ro
    restart: unless-stopped
